#include<bits/stdc++.h>
using namespace std;

int main(){

    list<int>l={60, 10, 20, 40, 60, 100, 100, 20};

    // l.remove(20);

    // l.sort();
    // l.sort(greater<int>());

    // l.unique();

    // l.reverse();

    for (int val : l)
    {
        cout << val << " ";
    }

    return 0;
}