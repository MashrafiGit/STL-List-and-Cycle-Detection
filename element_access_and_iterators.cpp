#include <bits/stdc++.h>
using namespace std;

int main()
{

    list<int> l = {1, 2, 3, 4, 5, 6};

    // operations
    // cout << l.front() << endl;
    // cout << l.back() << endl;
    // cout << *next(l.begin(), 2) << endl;

    // iterators
    cout << *l.begin() << endl;
    cout << *l.end() << endl;
    return 0;
}